package org.example;

public class ex1 {
    /*
ED: num, somaNumeros, aux INTEIRO
    continuar: CARACTERE

INICIO

    REPETE
        REPETE
            ESCREVER("INSIRA UM NÚMERO INTEIRO POSITIVO")
            LER(num)
        ENQUANTO(num <= 0)

        aux <- num
        somaNumeros <- 0

        ENQUANTO(num > 0)
            somaNumeros <- somaNumeros + (num MOD 10)
            num <- num DIV 10
        FIMENQUANTO

        SE(aux % somaNumeros == 0) ENTÃO
            ESCREVER("É NÚMERO DE HARSHAD")
        SENÃO
            ESCREVER("NÃO É NÚMERO DE HARSHAD")
        FIMSE

        ESCREVER("Deseja inserir outro número? (s/n)")
        LER(continuar)

    ENQUANTO(continuar == "s" ou continuar == "S")

FIM

    */
}

//REFAZER AMANHA

